{
  "name": "verifysign",
  "version": "1.0.0",
  "description": "VerifySign - Document Verification and Signing Platform",
  "main": "index.js",
  "scripts": {
    "dev": "cd client && npm run dev",
    "build": "cd client && npm run build",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:ui": "vitest --ui",
    "test:security": "vitest run tests/security/",
    "test:security:watch": "vitest tests/security/",
    "test:coverage": "vitest run --coverage"
  },
  "keywords": [
    "supabase",
    "vercel",
    "nda",
    "document-signing"
  ],
  "author": "Manuel S.",
  "license": "MIT",
  "dependencies": {
    "@supabase/supabase-js": "^2.81.0",
    "stripe": "^19.3.0"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^1.6.1",
    "@vitest/ui": "^1.6.1",
    "typescript": "^5.9.3",
    "vitest": "^1.6.1"
  }
}
